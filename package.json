{
  "name": "semver-first-satisfied",
  "version": "1.0.0",
  "description": "Find minimum in an array of version that satisfies a semver range",
  "repository": "parro-it/semver-first-satisfied",
  "license": "MIT",
  "author": "parro-it",
  "scripts": {
    "test": "eslint . && node test | faucet"
  },
  "keywords": [
    "semver",
    "satisfies",
    "min",
    "first",
    "minimum"
  ],
  "engines": {
    "node": ">=0.10"
  },
  "eslintConfig": {
    "extends": "js"
  },
  "devDependencies": {
    "eslint": "^1.10.3",
    "eslint-config-js": "^3.1.0",
    "faucet": "0.0.1",
    "tape": "^4.2.2"
  },
  "files": [
    "index.js"
  ],
  "dependencies": {
    "semver": "^5.1.0",
    "semver-max": "^1.0.0"
  }
}
